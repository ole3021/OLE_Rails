<%= provide(:title, @user.name)%>
<div class="row">
    <asdie class="col-md-4">
        <section class="user_info">
            <h1>
                <%= gravatar_for @user %>
                <%= @user.name %>
            </h1>      
        </section>
    </asdie>
</div>
